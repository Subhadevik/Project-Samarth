services:
  - type: web
    name: project-samarth
    env: python
    plan: starter
    region: oregon
    pythonVersion: "3.11.9"
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: cd backend && python main.py
    healthCheckPath: /api/health
    envVars:
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: false
    domains:
      - project-samarth.onrender.com